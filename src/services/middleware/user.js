// import { Navigate } from "react-router-dom";


// export const resetPasswordMiddleware = (store) => (next) => (action) => {
//    const state = store.getState();
//    const isReset = state.user.isReset;
//   const user = store.user?.userData;
//   console.log(state);
  
//   if (window.location.href==='/reset-password') {
//     window.location.href= "/forgot-password";
//     console.log('xfvx');
//   }
//   return next(action);
// };
